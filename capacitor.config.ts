import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tinyfamilyexpenses.app',
  appName: 'tiny-family-expenses',
  webDir: 'www'
};

export default config;
